{
  "name": "@empiricalrun/evals",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@empiricalrun/typescript-config": "workspace:*"
  }
}